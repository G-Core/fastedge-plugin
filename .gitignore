# Generated build artifacts — regenerated by release-plugin.yml workflow.
# Use `git add --force` to commit (release workflow does this automatically).
plugins/gcore-fastedge/docs-index.json
plugins/gcore-fastedge-codex/docs-index.json
plugins/gcore-fastedge-cursor/docs-index.json

# Mirrored reference content — copied from Claude plugin at release time by
# mirror-reference.sh. Committed via `git add --force` by the release workflow;
# do not hand-edit (overwritten on every release).
plugins/gcore-fastedge-codex/skills/*/reference/
plugins/gcore-fastedge-cursor/skills/*/reference/

# Cursor derived files — generated from the Claude plugin sources at release
# time by generate-cursor-plugin.mjs (a deterministic transform of CLAUDE.md and
# the Claude SKILL.md files). Committed via `git add --force` by the release
# workflow; do not hand-edit (overwritten on every release).
plugins/gcore-fastedge-cursor/rules/fastedge-knowledge.mdc
plugins/gcore-fastedge-cursor/skills/*/SKILL.md

# Release packaging output.
dist/
