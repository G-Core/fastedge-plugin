# Synthesis Instructions: react-with-hono-server-ts.md

> For shared output format, sections, extraction rules, and exclusions see [_scaffold-blueprint-http.md](./_scaffold-blueprint-http.md)

## Target file
`plugins/gcore-fastedge/skills/scaffold/reference/http/react-with-hono-server-ts.md`

## Frontmatter
```yaml
type: feature
app_type: http
languages: [typescript]
capabilities: [hono, react, vite, static-assets, dual-build]
base_skeleton: http-base
source_example: FastEdge-sdk-js/examples/react-with-hono-server
```

## Example-specific extraction hints

- This blueprint ships a **complete working React+Vite+Hono+FastEdge starter** — both halves of the dual-build pipeline. The user runs `npm install && npm run build` and gets a deployable WASM with the React bundle embedded
- The manifest ships 21 files; reproduce them verbatim in **Files to Create**, grouped by role:
  - **Vite/React root** — `index.html`, `vite.config.ts`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`
  - **React app source** — `src/main.tsx` (createRoot entry), `src/App.tsx` (demo UI with API fetch), `src/App.css`, `src/index.css`, `src/vite-env.d.ts`, `src/utils/api.ts` (fetch helper used by App.tsx), `src/assets/react.svg`, `public/vite.svg`
  - **FastEdge edge handler** — `fastedge-server/server.ts` (Hono app with API mount + static catch-all), `fastedge-server/api/routes.ts` (demo `/hello`, `/users` GET+POST, `/status` routes), `fastedge-server/config/server.config.ts` (`createStaticServer` runtime config), `fastedge-server/config/build-config.ts` (fastedge-build CLI config), `fastedge-server/dev-server.ts` (local Hono+`@hono/node-server` dev API), `tsconfig.fastedge.json` (scoped to `fastedge-server/`, excludes dev-server)
  - **Generated, ship verbatim then noted as transient** — `fastedge-server/config/asset-manifest.ts`. Preserve its `DO NOT EDIT — Generated by @gcoredev/FastEdge-sdk-js` banner. Note in **Build Notes** that `fastedge-build` overwrites this on every server build from `./dist/`; the shipped version is a starter so `tsc --noEmit` and editor TS server succeed before first build
- Preserve the full `scripts` block from the source `package.json` — it defines the dual-build workflow:
  - `dev` / `dev:vite` / `dev:api` — `npm-run-all -p dev:vite dev:api` runs Vite (port 5173) and the local Hono API (port 3001) in parallel; Vite proxies `/api` to 3001
  - `build` / `build:client` / `build:server` — `tsc -b && vite build` writes `./dist/` (the React bundle), then `npx fastedge-build --config ./fastedge-server/config/build-config.ts` embeds `./dist/` into the WASM via the asset manifest
  - `lint`, `preview` — standard Vite scaffold scripts
- **Dependencies to Add** (over `http-base`): runtime `"@gcoredev/fastedge-sdk-js": "^2.3.0"`, `"hono": "^4.9.8"`, `"react": "^19.1.1"`, `"react-dom": "^19.1.1"`; dev `"@eslint/js": "^9.36.0"`, `"@hono/node-server": "^1.19.5"`, `"@types/node": "^24.6.0"`, `"@types/react": "^19.1.16"`, `"@types/react-dom": "^19.1.9"`, `"@vitejs/plugin-react": "^5.0.4"`, `"eslint": "^9.36.0"`, `"eslint-plugin-react-hooks": "^5.2.0"`, `"eslint-plugin-react-refresh": "^0.4.22"`, `"globals": "^16.4.0"`, `"npm-run-all2": "^8.0.4"`, `"ts-node": "^10.9.2"`, `"tsx": "^4.20.6"`, `"typescript": "~5.9.3"`, `"typescript-eslint": "^8.45.0"`, `"vite": "^7.1.7"`
- The Hono edge handler pattern (`fastedge-server/server.ts`): `app.route('/api', api)` mounts the API routes; `app.get('*', async (c) => staticServer.serveRequest(c.req.raw))` is the SPA catch-all that serves the Vite bundle; wire via `addEventListener('fetch', (event: FetchEvent) => event.respondWith(app.fetch(event.request)))`
- Two `createStaticServer` configurations appear in the source — show the runtime config from `server.config.ts` verbatim (`type: "static"`, `notFoundPage: "/404.html"`, `spaEntrypoint: "/index.html"`, `autoIndex: ["index.html", "index.htm"]`) so SPA fallback and 404 behavior are explicit
- The local dev server (`fastedge-server/dev-server.ts`) is intentionally excluded from `tsconfig.fastedge.json` — it runs under Node via `tsx/esm`, not in the WASM. Call this exclusion out so users understand why CORS/`@hono/node-server` imports there don't bloat the edge bundle
- `vite.config.ts` configures `server.proxy['/api']` → `http://localhost:3001` so the dev Vite server forwards API calls to the local Hono dev server — preserve this so `npm run dev` works out of the box
- **Build Notes** must state: (1) the build pipeline is two-phase — Vite first, then `fastedge-build`; (2) `dist/` is the contract between the two phases; (3) `asset-manifest.ts` is overwritten by `fastedge-build` on every server build; (4) the local dev flow needs both `dev:vite` and `dev:api` running for the `/api` proxy to resolve
- "When to Use" hint: user wants a complete React+Vite+Hono starter where a single repo builds both a client bundle and a FastEdge WASM that embeds and serves it, with a parallel local dev flow for the React UI and the Hono API
